# 산냥이집냥이 (mohocat) — Engineering Hand-off (#3, Phase C design **batch**)

**Date:** 2026-06-21 · **Branch:** `dev` · **Last commit:** `c47724b`
(redesign Phase D) · **Work since:** **uncommitted** (see §6 — user commits next)
**Companions:**
[`2026-06-21-kickoff-3.md`](./2026-06-21-kickoff-3.md) (orientation + the tech-debt
map) · [`2026-06-21-handoff-2.md`](./2026-06-21-handoff-2.md) (Phase B/D) ·
[`docs/planning/PROJECT_PLAN.md`](../planning/PROJECT_PLAN.md) (project-wide tracker) ·
[`docs/design/mohocat-app-redesign-tasks.md`](../design/mohocat-app-redesign-tasks.md)
(per-chunk detail — the live source of truth).

---

## 0. ⚠️ Read this first — design work is **not** done

This hand-off marks the **end of a design _batch_**, not the end of design work.
The user is intentionally pausing the design pass here to **commit** and then
**pivot to functional work** (PROJECT_PLAN §11+). Several design items remain and
are listed in §3 — do **not** treat Phase C as complete.

---

## 1. What this batch did (Phase C, chunks 1–4 + terminology)

All `tsc`-clean and **browser-verified on logged-out surfaces**; states behind a
real sign-in are carried in A4 (the assistant can't enter credentials).

**Per-page restyle (brand cleanup + localization):**

- **동참 / contact** (`app/pages/contact/page.tsx`) — finished the activation
  (removed the `grayscale opacity-50 pointer-events-none` wrapper the overlay
  removal had left); focus rings `yellow-400` → `brand-300`; intro now links
  **"집사등록"** → `/login?tab=signup`; **보내기 is members-only** (disabled via
  `useAuth().isAuthenticated`, `handleSubmit` guard, logged-out prompt) — **UI gate
  only, not server-enforced** (PROJECT_PLAN §11).
- **FAQ** (`components/FAQ.tsx`, `app/pages/faq/page.tsx`) — accordion headers no
  longer use the CTA gradient on every item (neutral; open = `bg-brand-50` +
  brand-600 chevron + `text-ink`); title brand underline; `bg-blue-50 / "TBD."`
  box → brand-tinted card + "문의하러 가기" CTA to 동참. **Also moved FAQ in the nav
  from 소식 → 집사메뉴** (butler-facing) — `Navigation.tsx`, desktop + mobile.
- **공지 / announcements** (`app/pages/announcements/{page,[id]/page}.tsx`,
  `components/AnnouncementClient.tsx`) — pagination de-gradient-ified (current page
  = solid `bg-brand`, dropped `border-yellow-500`; prev/next = neutral secondary,
  previous/next → 이전/다음); Korean empty state; list cards; detail notice box
  `yellow-*` → brand-tinted card; back button → secondary token.
- **소개 / about** (`app/pages/about/page.tsx`) — localized residual English
  (사진을 불러오는 중… / 사진을 불러올 수 없어요 / 소개 / 내용을 불러올 수 없어요); the h1
  inline `style={{color: theme.primaryColor}}` (a **low-contrast yellow title**)
  → neutral `text-gray-900` + brand underline; removed dead commented blocks +
  the now-unused `theme` / `getMountainTheme` import.

**Terminology — 회원 → 집사 (cozier participation):** `constants/strings.ts` +
`app/admin/page.tsx`.

- **회원가입 → 집사등록** (signup heading, tab, complete, loading, error) and the
  related join verbs **가입 → 등록** ("등록한 계정", "집사 등록을 하신 뒤", etc.).
- Admin dashboard "회원" tile → **집사**. No "회원" remains in `src/`.
- Nav auth button label `strings.auth.nav.logIn` → **"로그인/등록"** (user shortened
  from "로그인/집사등록"); rendered by `NavigationBarLogin.tsx` (desktop + mobile).
- Codified in `design.md` → Language & Voice (avoid 회원; signup = 집사등록).

---

## 2. Quality gates

- `npx tsc --noEmit` — **clean** after every chunk.
- Browser-verified (logged out, localhost:3000): contact (active form, gated
  button + prompt, 집사등록 link → signup tab), FAQ (open/closed + card),
  announcements (list + detail), about (title + underline + photo + content), the
  login page tabs (로그인 / **집사등록**), the nav button ("로그인/등록"), 소식 dropdown
  (FAQ removed).
- **Not driven live (need sign-in / SMS → A4):** logged-in 보내기 enabled state,
  FAQ under 집사메뉴 (login-gated dropdown), mypage, the post-auth modals.

---

## 3. Design work that REMAINS (do not skip)

**Phase C — not yet done** (tracked in `mohocat-app-redesign-tasks.md` Phase C):

- **집사메뉴 / butler pages** — `butler_stream`, `butler_talk` (+ their `new/`
  forms, `ButlerStreamClient`, `ButlerTalkClient`, `PostList`): `border-yellow-500`
  selected state + a broader form/list brand audit. _Larger chunk._
- **입양홍보 / adoption gallery** — straddles design + functional; it's a **feature**
  (needs a `Cat.adoptable` flag + admin tagging + the public gallery page), so it's
  scheduled with the functional work (PROJECT_PLAN §11 / tasks C0).
- **Cross-cutting cleanup** — legacy blue `.btn-primary` in `globals.css`; the nav
  `hover:text-blue-600` on `NavigationBarLogin` (one token; deferred from D2).

**A4 — live-verification backlog** — the gated states in §2 + the prior auth modals
(`PasswordReset`, `UserNotFound`, `EmailVerification`, `KakaoLoginGuidance`,
`AnnouncementModal`). Need a real sign-in; the user can drive these.

**Beyond Phase C** (PROJECT_PLAN placeholders) — **Mobile UX** (§4), **Admin
desktop cleanup** (§5), **Admin mobile** (§6) are still 🚧 design-ish workstreams
not started.

---

## 4. The functional pivot (what the user does next)

Entry point: [`PROJECT_PLAN.md`](../planning/PROJECT_PLAN.md) **§11 — Functional
gaps**:

- **입양홍보 page** — build the adoptable-cats gallery (`Cat.adoptable` flag → admin
  tagging → public gallery on `components/album/*`). Until built, the 소식 link +
  brand CTA **404**.
- **동참 form end-to-end** — verify a submission actually writes `contacts` and
  reaches a human; decide whether to make the members-only gate **server-enforced**
  (Firestore rules requiring `request.auth`) rather than UI-only.

Broader functional/quality workstreams are scoped in PROJECT_PLAN §7–§10 (error
handling, route auth, RBAC collection drift, testing) — see kickoff-3 §5 for the
debt list.

---

## 5. Conventions (unchanged — carry forward)

- **Verify in a real browser** (`localhost:3000` via the Chrome extension); the
  `navigate` tool lags one step — re-screenshot to get the settled page.
- **Design source-of-truth = `design.md`**; token _values_ only in
  `tailwind.config.js`. Brand for actions/markers; neutral surfaces; **don't use
  brand-yellow as text color on white** (low contrast — use a neutral title + the
  short `bg-brand` underline accent, the motif used across FAQ/공지/about).
- **All user-facing copy = Korean (해요체)**, centralized in `constants/strings.ts`;
  **회원 → 집사** (signup = 집사등록).
- Keep `tsc --noEmit` clean; work in small browser-verified chunks; update
  `design.md` + the tasks doc per chunk.
- Media is build-fetched (`npm run fetch:assets`); single shared Firebase dev+prod
  (admin/console edits hit prod instantly).

---

## 6. Uncommitted state (user commits next)

Branch `dev`, on top of `c47724b`. Suggested logical commits:

1. **Phase C design batch (code)** — `src/`:
   `app/pages/{contact,faq,about,announcements/page,announcements/[id]/page}.tsx`,
   `components/{FAQ,AnnouncementClient,Navigation}.tsx`,
   `constants/strings.ts`, `app/admin/page.tsx`.
2. **Docs** — `docs/design/{design.md, mohocat-app-redesign-plan.md,
mohocat-app-redesign-tasks.md}`, `docs/planning/PROJECT_PLAN.md`,
   `docs/handoff/2026-06-21-kickoff-3.md`, this hand-off.
3. **⚠️ Pre-existing docs reorg (NOT this session)** — the working tree also has a
   large set of **deleted** `docs/**` files plus an untracked `docs/archive/`. That
   looks like an earlier docs cleanup/move that predates this session. **Decide
   separately** whether to commit it (e.g. `chore: archive obsolete docs`) — don't
   let it ride silently inside the design commit.

---

## 7. Resume here

1. **Commit** (per §6).
2. **Functional work:** open PROJECT_PLAN **§11** with the user; most likely start
   with the **입양홍보 adoptable-cats gallery** (`Cat.adoptable` → admin tagging →
   gallery) and/or the **동참 form server gate**.
3. When design resumes: the remaining Phase C items in §3 (butler pages →
   cross-cutting cleanup), then A4 live-verification with a signed-in session.

Small, browser-verified chunks; keep `tsc` clean; update `design.md` + the
plan/tasks docs as you go.
