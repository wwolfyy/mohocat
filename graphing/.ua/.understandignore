# Scope for the code-graph tooling evaluation:
#   docs/planning/pending/code-graph-tooling-evaluation-20260728.md
# Corpus is src/ + config/ only, matching the Graphify and CodeGraph runs.
# Explicit excludes rather than allowlist negation, so scoping does not depend on
# whether this matcher supports `!` re-inclusion.
# This file is inside .ua/, which is gitignored.

# --- SECURITY: never analyze the Firebase service-account private key ---
# config/firebase/mountaincats-*.json is a live credential. Excluded explicitly
# rather than relying on the tool's own secret heuristics or .gitignore handling.
config/firebase/

# --- out-of-scope top-level trees ---
_infra/
backups/
docs/
log/
public/
scripts/
test-results/
tests/
tmp/
.github/
.husky/
.claude/

# --- other tools' generated output ---
graphify-out/
codegraph-out/
.codegraph/
.ua/
node_modules/
.next/

# --- root-level tooling config (not app code) ---
playwright.config.ts
vitest.config.ts
vitest.rules.config.ts
next.config.js
postcss.config.js
tailwind.config.js
eslint.config.mjs
.lintstagedrc.js
test-kakaotalk-auth.js
next-env.d.ts

# --- root-level manifests and docs ---
# Excluded for evaluation parity: Graphify's allowlist-by-negation admitted only
# src/ + config/, so no root file entered its corpus. README.md and especially
# AGENTS.md (the full architecture guide) would otherwise let this tool describe
# the architecture from prose rather than derive it from code — which is exactly
# what the probe questions are meant to test.
package.json
package-lock.json
tsconfig.json
tsconfig.tsbuildinfo
firebase.json
.firebaserc
.env.test
.env.example
.nvmrc
.prettierignore
.prettierrc
.eslintrc.json
README.md
AGENTS.md
CLAUDE.md
