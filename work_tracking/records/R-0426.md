# R-0426 — Phase 5 — un-pause application work; the successor workstream is still unnamed

> **task · open** · 2026-08-09
> Migrated from `work_tracking/MIGRATION_JOB.md` — "Definition of done"
> (`work_tracking/MIGRATION_JOB.md#definition-of-done@44e08fe`).

---

**Application work has been paused since 2026-08-08 by owner decision**, in favour of the
work-tracking restructure. This record is the other half of the last box in the migration's
definition of done, and it follows `R-0425` — un-pausing while the orientation still describes the
old structure would restart feature work against the wrong map.

⚠️ **Tenancy T0 was deferred, not cancelled.** It was asked for and then stood down for this.
**No T0 work exists** — no spec written, no fixture touched — so it starts clean from
[`docs/planning/pending/tenancy-path-migration-plan-20260728.md`](../../docs/planning/pending/tenancy-path-migration-plan-20260728.md)
§3. Its context is `R-0345`, `R-0346` and `R-0395`.

**Also part of un-pausing:** the pause notice at the top of `AGENTS.md` (and therefore
`CLAUDE.md`) comes out in the same change, and the open list should be re-read before picking the
next thing up — `checkout.js --query "status = 'open'"` is the authoritative answer, not any list
written in a document.

---

## `rev 2` (2026-08-13) — the owner half-answered this on 2026-08-09, and T0 was ruled out

⚠️ **This record's original title said "tenancy T0 resumes". The owner has explicitly declined
that.** Asked on 2026-08-09 whether to un-pause and what should resume, the answer was
**"un-pause, but a different workstream next"**, followed by "I have a couple of things related to
work tracking, I'll explain further". Those turned out to be the work that became `R-0439` through
`R-0452`, and **the successor workstream was never named**.

**So the state is partial, not untouched.** Two of the three questions this record asks are
answered: application work should un-pause, and it should not resume with tenancy T0. The third —
what does resume — is still open and is the only thing blocking it.

🔑 **Recording this is the point of R4.** The exchange left no artifact. No file changed, no box was
ticked, and the next session would have read a title asserting T0 and started work the owner had
already turned down. That is the failure mode R4 exists for: a decision that leaves nothing behind
is indistinguishable from a decision never taken.

📌 **The note in this record's earlier revision, and the "Tenancy T0 is the presumed answer" line in
`HANDOFF.md`, both predate that exchange.** Treat them as superseded rather than as competing
opinions.
