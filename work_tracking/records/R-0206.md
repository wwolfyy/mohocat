# R-0206 — On-demand path: POST /api/revalidate wired to every admin cat-write, so edits reflect without a redeploy

> **task** · done · 2026-08-08 · §7a
> Migrated from `docs/planning/PROJECT_PLAN.md` (`docs/planning/PROJECT_PLAN.md#L772@f66997c`).

---

- [x] **On-demand path:** `POST /api/revalidate` (ID-token auth) wired to every admin
      cat-write in `src/app/admin/cats/page.tsx`, so edits reflect without a redeploy
      (end-to-end check pending a preview deploy — dev can't exercise ISR).
