# R-0425 — Phase 5 — rewrite `CLAUDE.md` and `AGENTS.md` to describe the new structure

> **task · open** · 2026-08-09
> Migrated from `work_tracking/MIGRATION_JOB.md` — "Definition of done" and the migration plan's
> Phase 5 (`work_tracking/MIGRATION_JOB.md#definition-of-done@44e08fe`).

---

**`CLAUDE.md` and `AGENTS.md` still describe the old structure.** Their orientation bullets name
`docs/planning/BACKLOG.md`, `log/DEBUG_LOG.md` and `log/FEATURE_MOD_LOG.md` as live sources; all
three are stubs. Until this lands, **the stubs are what redirect a reader — trust the stub, not
the bullet.**

**What has to change:**

- The `docs/` and `log/` orientation bullets, which name files whose roles have changed.
- The instruction to log a bug fix to `DEBUG_LOG.md` and an intentional change to
  `FEATURE_MOD_LOG.md`, plus the "which log?" rule that goes with it — all three are now one
  record separated by `type`.
- The obsolete "move an item and delete its origin in the same change" routing rule between
  `BACKLOG.md` and `PROJECT_PLAN.md`: nothing moves between files any more, because a status
  change is a field.
- The 🚧 pause notice at the top, which `R-0426` finishes.

⚠️ **This is the last step and should not start early.** Rewriting the orientation while Phase 3
is open points every future agent at a structure that is still moving. 📌 **`CLAUDE.md` is a
symlink to `AGENTS.md`** — there is one file to edit, not two.

**The gates to run with it:** `npx tsc --noEmit` and `npm run test:smoke`, plus the work-tracking
pair (`node work_tracking/tests/run.js`, `node work_tracking/scripts/build.js --check`).

---

## ✅ Done — 2026-08-09, with `R-0428`

`AGENTS.md`'s orientation section is rewritten around the registry. It now opens on
`work_tracking/HANDOFF.md`, gives the check-out → check-in → build loop as runnable commands,
and names `SCHEMA.md`, `registry.md` and `PROJECT_PLAN.md` at their **final** paths — which is
why `R-0428` rode along.

**What went, and what replaced it:**

- The 🚧 "this structure is being replaced" banner, and the `docs/handoff/` and
  `docs/planning/PROJECT_PLAN.md` + `BACKLOG.md` orientation bullets.
- The `log/` bullet and its _"which log?"_ rule — `DEBUG_LOG` vs `FEATURE_MOD_LOG` is now a
  `type` field, so the question does not arise. `log/doc_updates.log` survives as the one live
  file there and is named as such.
- The "Debug log" and "Feature mod log" instructions under **Testing & Debugging**, replaced by
  "write a record with `type: bug`" and ⚠️ **not** those files.
- The obsolete "move an item and delete its origin in the same change" routing rule, stated
  explicitly as no longer applying to anything: **a status change is a field, so nothing moves
  between files.**

**Added, because they are rules an agent needs _before_ it starts rather than after CI objects:**
the 📏 size policy, ⚠️ never `grep` the store (see `R-0438` — the rule stands, the figure that
used to be quoted for it does not),
⚠️ `work.json` is gitignored and must not be committed, and the work-tracking gates alongside the
application ones in the **Ask before committing** agreement.

⚠️ **The pause notice was deliberately KEPT, narrowed, and given an owner.** Deleting it would
have made this rewrite un-pause application work as a side effect, and that is `R-0426` — a
statement about what happens next, which is the owner's to make. The notice now says so in as many
words: _"Removing this notice is that record's job, not a side effect of some other change."_

📌 `CLAUDE.md` is a symlink to `AGENTS.md`, so this was one file to edit, as the record predicted.

**Verified.** `npx tsc --noEmit` 0 · `npm run test:smoke` green · the three store gates ·
a repo-wide link check showing 0 newly broken links.
