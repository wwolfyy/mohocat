# R-0282 — N1 — two permissions, write-own-post-butler / write-own-post-feeding, each covering create and edit-own

> **task** · done · 2026-08-08 · §10n
> Migrated from `docs/planning/PROJECT_PLAN.md` (`docs/planning/PROJECT_PLAN.md#L1943@f66997c`).

---

- [x] **N1 — two permissions**, `write-own-post-butler` / `write-own-post-feeding`, each
      covering create **and** edit-own on that board. `butler-internet` gets only the 집사톡
      one, preserving the per-board split the roles already encoded.
