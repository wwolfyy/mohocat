# R-0408 — `PROJECT_PLAN.md` and `HANDOFF.md` move into `work_tracking/` — owner-confirmed, and still not done

> **decision · done · adopted** · 2026-08-08
> Migrated from `work_tracking/MIGRATION_JOB.md` — "Decisions made during execution" and "Open
> questions for the owner" (`work_tracking/MIGRATION_JOB.md#D-plan-and-handoff-move@44e08fe`).

---

**This record was the tracker's only owner question, and its answer.** The question — _"Do
`PROJECT_PLAN.md` and `HANDOFF.md` move into `work_tracking/`?"_ — was raised on 2026-08-08 and
answered **✅ yes, owner-confirmed** the same day. Both are work-tracking artifacts, so they
belong with the rest of the work-tracking material rather than under `docs/`.

📌 **It is filed as one `decision`, not as a `question` row plus a duplicate decision row.**
`SCHEMA.md` §4 says a `question` "becomes something else once it is [answered]", and the
de-duplication rule this same pass imported (`R-0416`) says to import a decision only where its
reasoning is recorded nowhere else. Two rows for one settled fact would have broken both.

⚠️ **The decision is settled; the move has not happened.** `PROJECT_PLAN.md` is still at
`docs/planning/PROJECT_PLAN.md` and `HANDOFF.md` is still at `docs/handoff/HANDOFF.md`, and **no
phase of the migration plan schedules the move** — Phases 1–5 were checked for it. That gap is
now `R-0428`, open, so the decision cannot sit answered and unexecuted with nothing tracking it.
