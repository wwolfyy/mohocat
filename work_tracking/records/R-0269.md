# R-0269 — The '제목 없음' filler is gone from both places it lived — it announced a missing title over a description

> **task** · done · 2026-08-08 · §10i
> Migrated from `docs/planning/PROJECT_PLAN.md` (`docs/planning/PROJECT_PLAN.md#L1705@f66997c`).

---

- [x] **The `|| '제목 없음'` filler is gone from both places it lived** — it announced a missing
      _title_ while rendering a _description_, and the second instance sat in `VideoPlayer` under a
      player already showing the real title.
