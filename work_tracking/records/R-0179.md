# R-0179 — Batch-tagging UIs (tag-images / tag-videos) — the grid + multi-select + CatSelectorModal flow on touch; or explicitly…

> **task** · open · 2026-08-08 · §6
> Migrated from `docs/planning/PROJECT_PLAN.md` (`docs/planning/PROJECT_PLAN.md#L377@f66997c`).

---

- [ ] **Batch-tagging UIs** (`tag-images` / `tag-videos`) — the grid + multi-select + `CatSelectorModal` flow on touch; or explicitly mark desktop-only.
