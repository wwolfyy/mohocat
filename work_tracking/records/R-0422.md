# R-0422 — Phase 3 — repoint the 18 cross-references that still aim at stubbed files

> **task · done** · 2026-08-09
> Migrated from `work_tracking/MIGRATION_JOB.md` — "🔴 Found during Phase 2"
> (`work_tracking/MIGRATION_JOB.md#phase2-crossrefs@44e08fe`).

---

**18 references across 15 records** point, from inside migrated prose, at files that are now
stubs: `DEBUG_LOG.md` ×10, `PROJECT_PLAN.md` ×3, `FEATURE_MOD_LOG.md` ×3, `HANDOFF.md` ×2.

They **still resolve** — each stub names the record range it was migrated into and links to
`registry.md` — so this is not broken, only one hop longer than it should be. They should point at
record ids.

⚠️ **Deliberately not patched during Phase 2.** Rewriting them edits prose that `source_ref` pins
to a commit, which is the audit trail the whole bulk import rests on. Repointing a reference is a
judgment call about the text, so it belongs with Phase 3's de-duplication rather than with a
mechanical sweep.

📌 **Several of these are the same cross-file duplication Phase 3 has to resolve anyway** — the
reference and the duplicate tend to be the same sentence, so doing this alongside `R-0420` is
cheaper than doing it separately.

## Done 2026-08-09 — 20 repointed, 4 left alone

🔑 **The pinned prose was not rewritten.** Each reference **gained** a parenthetical naming the
record that now holds the content, so every original word survives and `git show <pin>` still
corresponds. Adding is the low-risk form of this edit; replacing the filename outright would edit
the text `source_ref` pins to a commit, which is the whole audit trail. Where the date carried
information it was kept — `` Full chain: `R-0014` (`DEBUG_LOG.md` 2026-07-30) `` reads as
provenance now, not as a pointer.

**16 `DEBUG_LOG.md` references** → `R-0002` ×2 · `R-0008` ×2 · `R-0009` · `R-0011` · `R-0013` ×2 ·
`R-0014` ×2 · `R-0015` · `R-0022` ×3 · `R-0024` · `R-0030`.
**3 `FEATURE_MOD_LOG.md`** → `R-0098` ×2 · `R-0057`.
**1 `HANDOFF.md`** → its "Open threads" section no longer exists in the stub, so `R-0087` now
names the archived body it moved into.

⚠️ **Left alone on purpose, and this is the judgment half:**

- **Three `PROJECT_PLAN.md` references** (`R-0096`, `R-0098`, `R-0099`). **`PROJECT_PLAN.md` is
  not a stub** — it kept all its prose and only lost its checkboxes, so those references still
  resolve to real content. Repointing them would be wrong.
- **`R-0096`'s `HANDOFF.md` mention** is inside a list of the documents that change *updated*
  ("Docs — … Added —"). It is a historical statement about what that commit touched, and it stays
  true. A reference is not always a pointer.

📌 **The count was 18 across 15; it was 20 across 19.** The original tally was taken during
Phase 2, before the hand-off import existed — `R-0354` and `R-0385` came later and carry one each.
The rest is the `PROJECT_PLAN.md` group, which that tally counted as in-scope and this pass did
not.

🔴 **One mapping was wrong on the first pass and caught before writing.** `R-0385` and `R-0354`
share the identical anchor text _"Full write-up in `log/DEBUG_LOG.md`."_, and both were initially
pointed at `R-0011` (the Firestore transport stall). `R-0385` is the SMTP secret reaching the test
harness — `R-0002`. Exactly the failure `R-0421` warns about in its own domain: **a wrong target
is harder to spot than a missing one**, because the sentence still reads fine. The script requires
each anchor to match **exactly once** and writes nothing otherwise, which is what made the pass
reviewable line by line rather than a sweep.

📌 **No revisions were written.** Only prose files changed; every row is untouched, which is
precisely why `SCHEMA.md` §4 keeps prose outside the row — so it can diff as prose.
