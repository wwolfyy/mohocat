# R-0232 — Smoke/UI tests for critical public paths (map loads, gallery opens, login renders) —

> **task** · done · 2026-08-08 · §10
> Migrated from `docs/planning/PROJECT_PLAN.md` (`docs/planning/PROJECT_PLAN.md#L1232@f66997c`).

---

- [x] **Smoke/UI tests for critical public paths** (map loads, gallery opens, login renders) —
      delivered by the `public/` + `auth/` suites.
