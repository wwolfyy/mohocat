# R-0304 — O2 — post-service.updateReplyCount recounts instead of increment(1).

> **task** · done · 2026-08-08 · §10o
> Migrated from `docs/planning/PROJECT_PLAN.md` (`docs/planning/PROJECT_PLAN.md#L2174@f66997c`).

---

- [x] **O2 — `post-service.updateReplyCount` recounts** instead of `increment(1)`.
      `deleteReply` calls it too, so deleting a 급식현황 reply _raised_ the parent's count;
      집사톡's service already recounted.
