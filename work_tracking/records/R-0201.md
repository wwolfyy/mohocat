# R-0201 — Build pipeline — ✅ build no longer exports to GCS; Phase 2 aligned it to fetch-static-assets + next build

> **task** · done · 2026-08-08 · §7
> Migrated from `docs/planning/PROJECT_PLAN.md` (`docs/planning/PROJECT_PLAN.md#L708@f66997c`).

---

- [x] **Build pipeline** — ✅ `build` no longer exports to GCS (Phase 2 aligned it to
      `fetch-static-assets.js && next build`); the GCS exporter + admin push path were
      removed in Phase 3B Half A.
