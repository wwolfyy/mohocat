# R-0423 — Phase 4 — break up `PROJECT_PLAN.md`'s mega-cells

> **task · open** · 2026-08-09
> Migrated from `work_tracking/MIGRATION_JOB.md` — "▶️ Resume here", item 5, and the migration
> plan's Phase 4 (`work_tracking/MIGRATION_JOB.md#resume-5@44e08fe`).

---

`docs/planning/PROJECT_PLAN.md` is **332 KB** — larger than `HANDOFF.md` was — because **single
table cells run to thousands of words**. The multi-tenant row holds the entire M1–M8 history
inside one cell. That is unreadable for a human and unparseable for an agent, and it is not fixed
by the box import: the boxes are gone, but the prose they sat beside is untouched.

**The file's state after Phase 2:** 2,653 → 1,493 lines, all 42 headings and all 45 table rows
intact, with each section's box list replaced by a pointer carrying that section's roll-up
(`6 items — 0/6 done — now in the work registry`).

📌 **Phase 4's other half is already done.** Shrinking `HANDOFF.md` was pulled forward into the
Phase 2 import — see `R-0417`. `R-0424` is the third Phase 4 item, the size policy that stops
either file growing back.

⚠️ This is independent work: the migration plan's own sequencing note says Phase 4 can slot
anywhere after Phase 2, so it does not block or depend on the Phase 3 records.

---

## ✅ Done — 2026-08-09

**The snapshot table is an index again.** All 30 rows now carry a status and one or two sentences
naming where the detail lives — the numbered section, or a record id. The file went **228 KB →
106 KB** and its longest line **4,528 → 399 characters**.

🔑 **Half the file was whitespace, and that was a symptom rather than the disease.** Prettier pads
every cell in a markdown table to the width of the widest one, so the multi-tenant cell's 4,350
characters were being **replayed as padding on all 29 other rows**: 48.1 % of the file was runs of
spaces, now 7.4 %. Shortening the one mega-cell is what collapsed the file, not any reflow setting.

⚠️ **Nothing was deleted before it was found somewhere else.** Each cell over 400 characters was
checked against the section it points at and against the registry before being collapsed. §9's
prose already carried the whole M1–M8 history, `R-0050` already carried colour Phase 5, and the
five claims that appeared **only** in a cell (`defaultRole`, the `겨울집 관리` stub, the
rotate-notice fix, the backup round-trip, the no-GCS-bucket reasoning) were each confirmed present
in a record first.

📌 **Two stale claims were corrected rather than carried forward**, since a rewritten row that
repeats a false claim is worse than the mega-cell was:

- The codebase-health row said the owner-owed YouTube **P5.4** pass was _blocked_. P5.4 passed on
  2026-08-07 — the multi-tenant row two lines below it said so.
- §10u's heading read _"Phase 5 open"_. `df132d0` closed Phase 5 on 2026-08-08, **before**
  `f66997c`, the commit the import read the boxes from — so `R-0323` and its plan record `R-0343`
  were imported open and are now corrected to done. §10u's roll-up moves 6/8 → 7/8; `R-0324`, one
  browser confirmation of `/admin/*`, is genuinely still open.

🔴 **It found a defect: `R-0435`.** Ten records were truncated mid-sentence by the same Phase 2
import as `R-0429`, and four of the dropped tails are still sitting in `PROJECT_PLAN.md` as
orphaned paragraphs. Found the same way `R-0429` was — by opening a record instead of trusting its
row.
