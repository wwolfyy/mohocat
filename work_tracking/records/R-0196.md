# R-0196 — Structured logging — replace ad-hoc `console.*` with per-module loggers, and never log secrets

> **task** · open · 2026-08-08 · §7
> Migrated from `docs/planning/PROJECT_PLAN.md` (`docs/planning/PROJECT_PLAN.md#L666@f66997c`).

---

- [ ] **Structured logging** — replace ad-hoc `console.*` with per-module loggers
      (`logger.exception` on errors); never log secrets (the Kakao flow logs are
      verbose — 118 `console.*` calls in `auth-service.ts` alone).
