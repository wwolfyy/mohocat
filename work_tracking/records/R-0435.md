# R-0435 — The Phase 2 import truncated ten records mid-sentence, and left four of the tails orphaned in `PROJECT_PLAN.md`

> **bug · open** · 2026-08-09
> Found while executing `R-0423`, by opening `R-0186` rather than trusting its row.

---

**Symptom.** Ten record files end **mid-sentence**. `R-0186` stops at
`` (c) **Every sync reset 게시일** (`uploadDate: new `` — the backtick never closes, the
sentence never finishes, and the ten lines that described the fix are simply not there.

| Record    | Source                                | Ends at                                              |
| --------- | ------------------------------------- | ---------------------------------------------------- |
| `R-0186`  | `PROJECT_PLAN.md#L461@f66997c`        | ``(`uploadDate: new``                                |
| `R-0198`  | `PROJECT_PLAN.md#L684@f66997c`        | ``**All 10 `/api/admin/**`routes now call``          |
| `R-0209`  | `PROJECT_PLAN.md#L777@f66997c`        | ``zero references to`cats-static-data.json`/``       |
| `R-0214`  | `PROJECT_PLAN.md#L866@f66997c`        | ``non-fatal to signup (`assignedBy:``                |
| `R-0240`  | `PROJECT_PLAN.md#L1261@f66997c`       | ``per-mountain playlist filing.** `social``          |
| `R-0291`  | `PROJECT_PLAN.md#L2034@f66997c`       | ``allow create: if canWrite('upload-own-photo') &&`` |
| `R-0396`  | `HANDOFF.md#D-cross-origin-sessions-rejected@2e07e3e` | `resolves via the fallback rather than the Host` |
| `R-0397`  | `HANDOFF.md#D-kakao-needs-no-per-subdomain-uri@2e07e3e` | `its redirect URI is Firebase's fixed handler,` |
| `R-0398`  | `HANDOFF.md#D-butler-board-is-a-log@2e07e3e` | `an empty description now stays empty instead of being` |
| `R-0403`  | `HANDOFF.md#D-commits-are-owner-gated@2e07e3e` | `Incidental finds this`                    |

**Root cause.** The source markdown contains **continuation lines that start at column 0** inside
an indented list item — `` (`uploadDate: new `` ends a line and `` Date()`), reordering… `` begins
the next one with no indent. By the markdown spec the list item **ends there**, so an importer that
reads items structurally takes everything before the unindented line and nothing after it. The
import was faithful to the parse; the parse was faithful to text that was already malformed.
`.prettierrc` does not reflow prose (`proseWrap` defaults to `preserve`), so the bad indentation
survived every format run that might otherwise have repaired it.

🔑 **Two defects, not one.** The record lost its tail **and** the tail stayed behind in
`PROJECT_PLAN.md` as a free-standing paragraph that reads as broken prose. Four of those orphans
are still in the file, immediately after a roll-up pointer:

- after §7's pointer — `` > Date()`), reordering the **public** 영상첩… `` (`R-0186`'s tail)
- after §7a's pointer — `` > `points-static-data.json`/`feeding-spots-static-data.json`… ``
  (`R-0209`'s)
- after §10a's pointer — `` > .youtubePlaylistId`per mountain… `` (`R-0240`'s)
- after §10n's pointer — `` > uploadingAsSelf()`. 📌 **`cat_videos` deliberately gains nothing** ``
  (`R-0291`'s)

The six remaining tails are **not** in the working tree at all — their source files are stubs now.
They are recoverable only from the pinned commits, which is exactly what `source_ref` is for.

⚠️ **This is the same class as `R-0429` and it escaped for the same reason.** Row counts, the
determinism test and `build.js --check` all pass on a truncated record, because a truncated record
is still a **valid** record. `R-0429`'s repair added a real-store assertion for **empty** bodies;
nothing asserts a body is **complete**.

**Suggested repair.** Recover each tail with `git show <pinned commit>:<path>`, append it to the
record at `rev + 1`, and delete the orphaned paragraph from `PROJECT_PLAN.md` in the same change.
Then add the cheap detector that would have caught it to `work_tracking/tests/store.test.js`: a
record body whose backticks are **odd** or whose last line ends without terminal punctuation. It
finds all ten with no false positives beyond blockquote enders.

📌 **Roughly 5.8 KB of prose is missing from the six `PROJECT_PLAN` records** — measured by
differencing each record body against its item in `plan-before.md` at `f66997c`, so treat it as an
estimate of the order of magnitude, not an exact figure. The four hand-off records are unmeasured.
