# R-0285 — N4 — board pages gate on the view-post-* permissions, and the /new routes gained a gate at all

> **task** · done · 2026-08-08 · §10n
> Migrated from `docs/planning/PROJECT_PLAN.md` (`docs/planning/PROJECT_PLAN.md#L1951@f66997c`).

---

- [x] **N4 — board pages** gate on the `view-post-*` permissions; the `/new` routes gained a
      gate at all (they had none — reachable by URL, harmless only because the write failed).
