# R-0420 — Phase 3: lift the decisions held _inside_ the two decision documents as their own rows

> **task · open** · 2026-08-09
> Migrated from `work_tracking/MIGRATION_JOB.md` — "▶️ Resume here", item 2
> (`work_tracking/MIGRATION_JOB.md#resume-2@44e08fe`).

---

**Do this after `R-0419`.** Phase 2b indexed `multi-tenant-architecture-decision-20260718.md`
(`R-0339`) and `tenancy-url-model-decision-20260728.md` (`R-0346`) as **one pointer row each**.
That is right for the documents — `R-0406` settled that a long plan is a document, not a row —
but it leaves the decisions those documents contain **unqueryable**. A cold session asking "was X
considered?" finds a pointer to a file, not an answer.

**A third candidate turned up while reading:** `firebase-sdk-usage-inventory.md` (`R-0335`) closes
by retiring the blanket _"Admin SDK is the eventual target for all writes"_ framing. That is a
decision the migration plan had not listed, and it is noted on `R-0335`.

📌 **Apply the same rule as the hand-off pass** (`R-0416`): import a decision only where its
reasoning is recorded nowhere else, and **probe each candidate against the store before writing
it**, the way `handoff.js` documents. Four of the seven decisions the plan named turned out to be
already in the store; assume the same rate here.

⚠️ Indexing the document and lifting a decision out of it are not in conflict. The pointer row
stays; the new rows carry the conclusions and cite the document.
