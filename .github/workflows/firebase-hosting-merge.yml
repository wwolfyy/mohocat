# DEPRECATED: This workflow is disabled in favor of Cloud Run deployment
# See .github/workflows/cloud-run-deploy.yml for current deployment

# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# DISABLED - Firebase Hosting deployment replaced by Cloud Run
# Reason: Next.js image optimization requires server runtime
# New deployment: Google Cloud Run (see cloud-run-deploy.yml)

# name: Deploy to Firebase Hosting on merge
# on:
#   push:
#     branches:
#       - main
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - run: npm ci && npm run build
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MOUNTAINCATS_61543 }}
#           channelId: live
#           projectId: mountaincats-61543
